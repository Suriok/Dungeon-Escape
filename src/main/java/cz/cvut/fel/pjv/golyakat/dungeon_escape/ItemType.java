package cz.cvut.fel.pjv.golyakat.dungeon_escape;

/**
 * The {@code ItemType} enum categorizes all item types used in the game.
 * <p>
 * These types help determine how items behave during gameplay,
 * including usage (e.g., healing), equipping (armor, weapons), or crafting.
 * </p>
 */
public enum ItemType {
    HEALING,
    KEY_PART,
    KEY,
    ARMOR,
    WEAPON
}